# Local environment (secrets)
.env
.env.*
!.env.example

# OS / editor noise
.DS_Store
Thumbs.db
.idea/
.vscode/

.claude*
# local backup copies (never commit)
*.bak

# Temporary files for backup scripts
!keycloak/bootstrap.sh.bak

# Local mkcert dev certificates (per-machine, never commit)
traefik/certs/
